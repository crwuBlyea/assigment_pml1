# Stages 1–2 run on the host
pandas>=2.0
numpy
scikit-learn>=1.4
joblib>=1.3
mlflow>=2.11

# Automation
APScheduler>=3.10,<4.0

# NOTE: FastAPI / Streamlit / uvicorn are not needed on the host –
# they run inside the Docker containers (see code/deployment/*/requirements.txt)
